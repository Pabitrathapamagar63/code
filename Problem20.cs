using System;
namespace ConsoleApp
{
    public class Problem20
    {
        public static void solution()
        {
            int i, n=30, sum=0;
            for(i=1; i<=n; i++)
            {
                sum = sum + i;
            }
            Console.WriteLine(i);
        }
    }
}